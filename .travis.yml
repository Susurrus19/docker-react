language: generic
sudo: required
services:
  - docker

before_install: 
  - docker build -t kwellsetsy/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true kwellsetsy/docker-react npm run test

